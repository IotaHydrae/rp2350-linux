# 学习地图 · RP2350 Linux 移植

> 这份给 AI 续学用；人看习得去同文件夹 `学习记录/`。

## ⚡ AI 接管协议

1. 先读「现在在哪」→ 接在那一步；顺带读仓库根 `PLAN.md`（产品与工程现状以它为准）。
2. 守铁律：手是用户的手 / 一次一步讲完就停 / 委托人决策用户拍·代码 AI 写（写完直接让用户跑）。
3. 搭台前先抛一道图问（细则见 build-to-learn 的 references/3-通关.md）。
4. 看「认知模型待加固」，续学路上补。
5. 按「怎么重启环境」搭好台，交棒给用户。

## 📍 现在在哪

- 进度：S1 ✅；S2 ✅；S3-00 ✅；S3-01 ✅；S3-02 ✅；S3-03 ✅；S3-04 ✅；S3-05 ✅（进 shell）；**S4（文件系统篇）：S4-00 ✅；S4-01 ✅（shell 调用外部程序）；S4-02 ✅（ext2 真实文件系统 on brd）；S4-03 ✅（根切 ext2，legacy initrd）**
- 当前部件图：真源在 `学习记录/S4-02 · ext2真实文件系统.md` 顶部（终版）
- ⏸ 手正悬在哪：**S4-04 工具链构建中（2026-08-28，Buildroot）。** buildroot-2026.05.2 已解压到 `/home/developer/buildroot-2026.05.2`；defconfig 已配：`qemu_riscv32_nommu_virt_defconfig` 并裁剪掉内核/QEMU/rootfs 镜像（只留工具链+elf2flt+busybox；BR2_BINFMT_FLAT=y 时 gcc 依赖自动带出 host-elf2flt，toolchain wrapper 自动加 -Wl,-elf2flt）。**两个宿主环境坑**：① PATH 有带空格的 Windows 路径（/mnt/c/Program Files/...）→ 构建必须用干净 PATH；② 机器把 108 个 coreutils 换成 uutils（Rust 版），buildroot 硬拒 install → 用 `/tmp/brhostbin/install -> /usr/bin/gnuinstall` 符号链接 + PATH 前缀绕过（未改系统；用户自己跑 buildroot 也要这么处理）。构建命令：`cd /home/developer/buildroot-2026.05.2 && PATH=/tmp/brhostbin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin make -j14`，日志 `/tmp/buildroot-build.log`（**只看错误行，别整段看**）。下一步：构建完成 → output/host/bin/riscv32-buildroot-linux-uclibc-gcc + elf2flt → 编最小 busybox（buildroot output/build/busybox-*/ 源码）→ 转 bFLT → 塞我们自己的 ext2 → 量大小（超 512KB 扩 BLK_DEV_RAM_SIZE 重编内核）。S4-03 大考虚点（两层链/坑清单/验收行）登记待加固。
- ⚠️ 待办（QEMU 回归，2026-08-28 发现）：S3 时代内核（含 AMO 模拟器/条件原子 amocas 改动的任何构建，含 build-rv32 重建产物）在 QEMU virt 里起不来：第一个 trap 就是 earlycon 8250 读 UART LSR（0x10000014）load fault（cause=5，M-mode 下本不该 fault），随后 ebreak 风暴（irqentry_nmi_enter 的 WARN）→ handler 文本 0x80195ecc 处运行字节与 vmlinux 不符（0x94e4 vs dc3a，疑重定位写坏）。旧 S2 内核（Aug 21）在 QEMU 正常。**PC 侧迭代 /init 依赖修好这个**，未排期，记录待后续排查（怀疑点：amocas.w 在 QEMU 的非法指令路径 + try_amo_emulation 对非 PSRAM epc 直接放行 → die() 路径异常）。
- 关键更正（务必记住）：① GDB 的 mscratch=0x1120f3c0 是 OpenOCD 假象（正常启动 mscratch=0）；② get_cycles64 崩 = DTB 无 clint → clint_time_val=NULL（S3-02 修）；③ **LR/SC 在 PSRAM 不 fault**（手册 2.1.6：排他访问仅 SRAM，sc.w 静默失败）→ cmpxchg 强制走 amocas.w（AMO 编码会 fault 可模拟）；④ **RP2350 非对齐 32 位 load 返回 0xf0000000**；⑤ **nommu_virt_defconfig 自带 CONFIG_CMDLINE_FORCE=y 会覆盖 DTB bootargs**——已并入完整 defconfig；⑥ **不要用 merge_config 碎片**：alldefconfig 用默认值重建会打开 MMU，savedefconfig 会丢掉非默认的 ARCH_RV32I（base ISA 默认 RV64I）→ 每工程一份完整 defconfig（`make kernel-s3-0x`，见 `环境搭建.md`）；⑦ **bootloader 5 个 workaround 全冗余**（真板验证，`1b2e416`）：uncached 拷贝/手动 WRITABLE_M1（SDK psram_init 已置）/禁用 XIP 缓存/invalidate/清 mscratch+MIE——当时"写回缓存不可靠"实为 **PSRAM_CS 上拉硬件问题**。；⑧ **panic 停机循环每轮 mdelay(PANIC_TIMER_STEP)**（kernel/panic.c），GDB 抓 pc 在 `__delay` 是正常现象（用户 2026-08-28 查证），不是卡死。
 ⑨ **S3-02 条件原子改造（87546b2fd）的 `arch_atomic_dec_unless_positive` 返回值写反**——被 `deny_write_access` 使用，潜伏到 S3-05 首次 exec /init 才炸（-ETXTBSY）。**已修并提交内核（`62770bc00`）**；同类函数核对无误。
 ⑩ **Xh3irq chained handler 必须保存/恢复 MEICONTEXT**（`536c2f536`）：非 MEIP 陷阱（1ms 定时器）清 MRETEIRQ → mret 不弹优先级栈 → 外部中断第二次起永久屏蔽；单发 MEIFA force 验收发现不了。
 ⑪ **riscv32 无 wait4**（asm-generic 只给 time32/64 位）→ 等子进程用 waitid(95)；**waitid options 必须显式含 WEXITED**（传 0 得 -EINVAL）。
 ⑫ **bFLT 头 64 字节**（magic+10 字段+filler[5]），entry=64；ELF 必须 `-no-pie` 无重定位；无 libc 程序函数多时 `_start` 加 `.text.start` 段保证 .text 偏移 0。
- 代码现状快照：工程目录 `s3/00_amowall/`..`s3/05_shell/`、`s4/00_boot-initramfs/`、`s4/01_exec-hello/`、`s4/02_ext2/` 各带 bootloader（banner 识别工程）+ 完整 defconfig；`make kernel-s4-0x / init-s4-0x / rootfs-s4-0x / flash-s4-0x-*` 一键构建烧录（**约定：kernel-Image 只在重编时保留，未变部分复用前关 flash 目标**）。内核 linux-7.2 提交 10 笔（详见 `notes/内核改动记录与溯源.md`）。S3+ 内核开 CONFIG_DEBUG_INFO_DWARF5（行号可用）。rootfs 机制：S4-00 起 initramfs 由 bootloader 拷（DTB `linux,initrd-start/end` 固定约定 0x11300000-0x11400000），分区 2 = ROOTFS 1MB；S4-02 起 rootfs 里含 ext2 镜像（brd）。
- 排查脚本（scripts/，英文输出、vmlinux 第一参数）：`pc-locate.sh <vmlinux> <pc>` 反汇编/符号/源文件；`gdb-dump.sh <vmlinux>` 一键抓现场；`log-analyze.sh <vmlinux> <日志>` Call Trace 批量翻译带行号；`start-openocd.sh` 启动调试器；`verify-images.sh` 烧录校验；`diff-kernels.sh` 内核对比。
- 工程内复现材料：`s3/00_amowall/README.md`（含调试器抓现场指引）+ 本工程 kernel-Image（`make kernel-s3-00` 构建）；`s3/01_earlycon/README.md` + kernel-Image（`make kernel-s3-01`）
- S4-03 规划（2026-08-28 用户拍板方向，开工时展开）：根切 ext2——bootargs `root=/dev/ram0 rootfstype=ext2`；DTB `linux,initrd-*` 继续指向内存里的 ext2 镜像（不再是 cpio），内核走 **legacy initrd**（prepare_namespace → `initrd_load()` 拷进 /dev/ram0 → `mount_root()` 挂 root= 设备 → 执行根里 init）。待拍：镜像怎么进内存（bootloader 把分区 2 直接当 ext2 拷？还是保留 cpio 阶段拷贝）；legacy initrd 会打 deprecated 警告；CONFIG_BLK_DEV_RAM 已开。验收实锤 = `VFS: Mounted root (ext2 filesystem)`。
- QEMU 一键测试：`s2/run-qemu.sh`（参数：KERNEL_SRC / BUILD_DIR 可覆盖，额外参数透传给 qemu）
- 怎么重启环境：`cd /home/developer/iotahydrae/rp2350-linux`；`PICO_SDK_PATH=/home/developer/raspberrypi/pico-sdk`；工具链就绪后 `cmake -DPICO_PLATFORM=rp2350-riscv -B build -G Ninja`

## 🗺️ 要去哪

| 阶段 | 名字 | 跑成什么样 | 承重路标 | 状态 |
|---|---|---|---|---|
| S1 | 假镜像闭环 | bootloader 拷贝 + 跳转假镜像，串口打印确认 | PSRAM 初始化 + 跳转协议 | ✅ |
| S2 | QEMU 跑真内核 | 构建 riscv32 NOMMU 内核（MMU=n、M-mode），QEMU 里看到启动输出（哪怕 panic） | 内核构建 + 启动协议（入口/DTB/链接地址） | ✅ |
| S3-00 | AMO 墙定位 | 最小 DTB 真板引导，复现静默卡死，GDB 抓 mcause=7/amoor.w | 真内核加载 + 最小 DTB + 调试抓现场 | ✅ |
| S3-01 | earlycon 出字 + init_IRQ panic | AMO/amocas 模拟器跨墙，banner 打印后按设计 panic | 模拟器 + earlycon + panic 验收 | ✅ |
| S3-02 | 真板接定时器 | 定时器链：init_IRQ panic 消失、jiffies 动起来 | cpu-intc + timer-rp2350 驱动（1MHz tick，与 sys_clock 解耦） | ✅ |
| S3-03 | 真板接中断 | 内核能响应外设中断 | Xh3irq 驱动（CSR 数组 0xbe0-0xbe5，非标准 PLIC） | ✅ |
| S3-04 | 真板真 console | ttyAMA0 确定接管日志（bootconsole disabled），VT 恢复后不被抢 | 显式 console=ttyAMA0 + CONFIG_VT 恢复测试 + sbsa-uart 定案（S3-03 已拍 sbsa，本关定长期/过渡） | ✅ |
| S3-05 | 进 shell | 板子上敲命令 | initramfs（cpio 只含自写静态 /init shell 程序）+ init=/init | ✅ |
| S4-00 | bootloader 拷 initramfs | rootfs 独立分区烧录，改 rootfs 不用重烧内核 | 分区 2 + bootloader 拷 RAM + DTB linux,initrd-start/end + 内核去掉 baked-in | ✅ |
| S4-01 | shell 调用外部程序 | 敲 hello 由 /bin/hello 打印（不是 shell 自己打） | NOMMU 进程创建（vfork/execve/waitid）+ /bin/hello 独立 bFLT + rootfs 加 /bin | ✅ |
| S4-02 | ext2 真实文件系统 | 挂载 ext2（brd RAM 块设备），写/读文件验证 | brd + mkfs.ext2 镜像 + /init 拷镜像/mount/写读 | ✅ |
| S4-03 | 根切 ext2 | 内核直接挂 /dev/ram0 为根，执行 ext2 里的程序 | legacy initrd（linux,initrd-* 指向 ext2 镜像）+ root=/dev/ram + mount_root | ✅ |
| S4-04 | busybox 移植（riscv32 NOMMU，注意镜像大小） | rootfs 里跑真实 busybox（ash + 常用 applet） | 工具链（uClibc-ng + elf2flt，buildroot 或手搓）+ ELF→bFLT 转换（内核重定位表）+ 镜像尺寸控制 | 🔄 |
| S4-05+ | 文件系统篇其余内容（待定：rw 根、更多命令/工具、rootfs 写实验等） | 待定 | 待定 | ⬜ |
| S5 | 裁剪优化 | 系统更轻快：bootloader 频率调整、内核压缩、zram 等 | bootloader 时钟配置 + Image 压缩/解压 + zram/swap 概念 | ⬜ |
| S6 | RP2350 外设控制器适配 | i2c/spi/watchdog/dma/pio 驱动；i2c/spi 拿传感器或 SPI 屏验收 | 手册寄存器 + 内核现成驱动框架（i2c-designware 等）+ DTB + 最小验证；dma/pio 后置 | ⬜ |
| S7 | 双核 AMP + rpmsg（方向已拍 2026-08-28，阶段拆分待定） | core1 裸机/RTOS 程序 + Linux rpmsg 通信 | bootloader 起 core1 + SIO 邮箱 FIFO/共享内存传输 + rpmsg 框架；候选子阶段：core1 启动+FIFO 最小验证 → 共享内存协议 → rpmsg 传输 → 应用验收 | ⬜ |
| S8 | 电源管理（PM，方向已拍 2026-08-28，范围待拍） | 功耗/时钟可观察、可调 | 候选范围：CPU idle（WFI+tickless 可观察）、CCF 时钟树（时钟交回内核）、DVFS（测电流）；**不建议一上来做 suspend/deep sleep** | ⬜ |

- 认知模型待加固：
  - 「形状 vs 参数」抽象：迁移时哪些是结构、哪些是参数（S1 大考 Q2 没接住）→ S2/S3 真实施工练
  - 坑的排查顺序：先软件配置坑（地址/偏移/引脚），后硬件坑（S1 大考 Q3 先想到硬件）→ S2 练
  - 定时器/中断子系统 → S3-02 已接触（clocksource/clockevent/percpu irq/MTIP）；**S3-02 大考已补**（2026-08-28 迁移四问：判断/形状/验收过，偏虚 = 寄存器写入协议）
  - **UART 输入链的形状与证据（S3-04 大考浮出，S3-03 欠账并账）**：输入路径 = /init open tty → uart_startup 使能 RX 中断 → Xh3irq 分发 → handler 收进 tty 缓冲 → read()；console 只管输出不 open tty。键盘没反应先查 open + 中断链，不是文件系统；验收用中断链证据（回显 / handler 计数 / MEIFA force 对照）。→ S3-05 亲手写 /init 时补
  - **S3-05 大考残留**：Q1 没主动点出"输入链靠 /init open tty 通电"（tty open 依赖）；Q2 具体驱动框架形状（irqchip domain+chained handler、8250 现成驱动改 DTB）——都是亲手做过但没内化命名的部分，后续项目实做时补
  - ~~"仿照现有"而不说改哪一环~~ → **S4-01 大考已补**：主动说出 is_ls/run_ls 动作 + 真实 shell 的 PATH 抽象 + 坑概括为"查系统调用表、查参数约束"。
  - **根挂载流程（prepare_namespace → initrd_load → mount_root 分支）**：S4-02 大考 Q2/Q4 虚——"照什么形状"答成"放什么命令"、"怎么验"没到 `VFS: Mounted root (ext2)` 实锤行。S3-05 文档讲过被 initramfs 跳过的这个分支，但没内化 → S4-03 实做补。
  - **S4-03 大考残留（迁移四问）**：Q1 判断虚——"文件系统链是两层：识别魔数→拷盘 / mount 试挂"，且漏了配置层（BLK_DEV_RAM_SIZE、fs 编进内核）；Q3 定位虚——legacy initrd 具体坑清单（ram0 解析、image too big、无 /dev/console、No working init）；Q4 验收虚——实锤是具体行 `VFS: Mounted root (xxx filesystem) on device 1:0.` + Run init + shell 交互。→ S4-04 文件系统链继续用就补。
  - **SMP 结论（2026-08-28 用户问）**：riscv 的 SMP 机制（secondary boot、IPI、per-CPU 上下文）按 MMU+SBI 设计；NOMMU + M-mode 裸机内核开 SMP = 重写整套启动/中断分发，且无 MMU 就没有每核地址隔离。**不走 SMP**。
  - **AMP + rpmsg 可行性（2026-08-28 核实手册）**：RP2350 双核共享同一地址空间（SRAM/PSRAM 需画地盘）；SIO 有两个 32×4 邮箱 FIFO（core0→core1 / core1→core0）+ 32 个硬件自旋锁；MTIME 共享单计数器、MTIMECMP 每核一份（同地址每核私有）；Xh3irq 中断路由到两核、每核 enable/force 数组（我们驱动已摸过 MEIEA/MEIFA）。方案：bootloader 按 SDK multicore 协议起 core1 → core1 裸机/FreeRTOS → Linux 侧 rpmsg 传输（SIO FIFO 当 mailbox + 共享内存 vring，或自定义传输，参考 Qualcomm SMD 先例）。建议先做最小验证（FIFO 传一个字）再上 rpmsg，放 S7。
  - **外设寄存器写入协议细节**（如 MTIMECMP 必须先写全 1 防假中断；S3-02 大考补考漏答）→ 新外设移植先查手册"写入序列"章节
  - **rv32 的 64 位原子（cmpxchg64 走 LR/SC）未收口**：本次未触发；S3-03 之后若撞到需迁 amocas.d + 模拟器扩展（能力库已记边界）
  - DTB 生命周期与布局 → 已拍板并成文（`notes/DTB生命周期与布局分析.md`），S3 按此落地
- 手边比喻世界：（空）

## 🎯 这是什么

- 目标：Linux（riscv32 NOMMU）跑在 Waveshare RP2350 Plus 16MB 的 RISC-V 核上，串口进 shell
- 剧本：烧录 → 上电日志 → 里程碑（假镜像 → 真内核 → shell）
- 代码：`/home/developer/iotahydrae/rp2350-linux`（PLAN.md）；习得：`notes/学习记录/`
- 能力块：`../_能力库.md`（Documents 笔记根，跨项目）
- 用户画像：懂 Linux 驱动与 pico-sdk；移植经验为零；要求小步实验、每关记录 + git 提交；中文交流；会 picotool / openocd / 真板调试
- 用户约定：**需求/项目结构有变化时，同步更新仓库根 `AGENTS.md`**（2026-08-21 用户要求；AGENTS.md 由 `codex /init` 生成基础版 + AI 补充项目约定）
- 参考速查：`notes/RISC-V调试与反汇编速查.md`（异常三件套 + 反汇编命令 + 案例复盘）
- OpenOCD+GDB 教程：`notes/OpenOCD-GDB调试教程.md`（连接流程 / 断点单步 / 源码 / 寄存器 / TUI 分屏 / 抓第一现场实战）
- 参考速查：`notes/RISC-V汇编指令速查.md`（ARM 对照版：寄存器 / 指令表 / 压缩指令 / 反汇编阅读）
- 环境速查：`notes/环境搭建.md`（RISC-V 工具链下载/安装 + 完整构建环境）
- 内核文件系统链路：`notes/内核initramfs与rootfs启动链路详解.md`（rootfs/initramfs 解包/init_eaccess/console_on_rootfs/prepare_namespace 全链路）
- bFLT 用户态格式：`notes/bFLT格式与手搓打包详解.md`（NOMMU 无 ELF、bFLT 头/加载器/无重定位原理、pack-bflt.sh 操作与风险）
- 内核执行用户态程序：`notes/内核FLAT程序执行链路详解.md`（exec 调用链/do_open_execat 的 ETXTBSY 卡点/search_binary_handler/load_flat_file/start_thread）
- DTB 参考：`notes/QEMU-virt-DTB参考.md`（QEMU 动态生成，dumpdtb 导出；关键节点 ↔ 启动日志）
- DTB 生命周期：`notes/DTB生命周期与布局分析.md`（解析后数据已死但内存被内核永久保留；memory 节点 = 内核唯一内存信息来源；SRAM/SCRATCH/XIP_RAM 暂不声明、闲置；启动代码在 PSRAM 执行无原则问题、注意拷贝/缓存交接；earlycon 解析/打印/真 console 接管条件；只给 earlycon 时的启动死点链（intc/timebase panic、jiffies 冻结挂）→ S3 布局依据）
- 练习题：`exercises/`（S1/S2 知识题/代码题/程序题，答案折叠；S3+ 随阶段补充）
- 原始日志：`notes/实验日志/`（真机原始输出存档，带日期/板子/结论）
- S3-00 完整调试记录：`notes/学习记录/S3-00 · earlycon 工程：真板静默调试全过程.md`（搭建→烧录四坑→QEMU 二分→GDB 第一现场→AMO 墙→对照实验）
- S3-01 调试总结：`notes/学习记录/S3-01 · 内核AMO模拟器改动记录.md`（第 8 节：mscratch 假象/get_cycles64 更正/0xf0000000/CS 上拉怀疑）；排查日志：`实验日志/2026-08-25_S3-01硬件间歇性排查.md`
- **S3-01 完整踩坑复盘（从 AMO 墙到 earlycon 出字，五关日志全含）：`notes/学习记录/S3-01 · 从AMO墙到earlycon出字：完整踩坑历程.md`**
- **S3-02 定时器链复盘（clint 三硬伤 / 1MHz tick / percpu irq / dummy console / LR-SC 条件原子墙，五步真板日志全含）：`notes/学习记录/S3-02 · 定时器与中断链.md`**；原始日志：`实验日志/2026-08-27_S3-02定时器链验收.md`
- **S3-03 Xh3irq 外设中断复盘（CSR 数组 / MEIEA 幽灵位风暴 / AMBA 总线缺失改 sbsa / tty open 依赖 / 分发调用误删乌龙）：`notes/学习记录/S3-03 · Xh3irq 外设中断.md`**；原始日志：`实验日志/2026-08-28_S3-03外设中断验收.md`
- **平台移植手记（新平台写 timer/irqchip 驱动的通用方法论：调研清单、clocksource/clockevent 两角色、irq domain/chained handler、DTB 胶水、最小验证路径）：`notes/平台移植手记-timer与irqchip.md`**
- **AMO 模拟器教学详解（trap 路径当软件执行器 / 指令编码逐段拆解 / LR-SC 为什么静默失败 / 半字取指 / epc+4 原理 / 扩展指引）：`notes/内核AMO模拟器详解.md`**
- **内核改动总账（7 笔提交：做了什么/为什么/验证，全部带 commit id 可溯源，含已回退的临时补丁）：`notes/内核改动记录与溯源.md`**
- 内核裸串口诊断打印（PL011 直写，earlycon 之前的救命手段）：`notes/学习记录/S3-01 · 裸串口PL011诊断打印.md`

> 欠条：**S3-03 大考欠条已并账**（2026-08-28 并入 S3-04 通关大考：Q1/Q2 暴露 UART 输入链虚点，已登记待加固，S3-05 实做补）。
