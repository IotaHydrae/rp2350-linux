# 学习地图 · RP2350 Linux 移植

> 这份给 AI 续学用；人看习得去同文件夹 `学习记录/`。

## ⚡ AI 接管协议

1. 先读「现在在哪」→ 接在那一步；顺带读仓库根 `PLAN.md`（产品与工程现状以它为准）。
2. 守铁律：手是用户的手 / 一次一步讲完就停 / 委托人决策用户拍·代码 AI 写（写完直接让用户跑）。
3. 搭台前先抛一道图问（细则见 build-to-learn 的 references/3-通关.md）。
4. 看「认知模型待加固」，续学路上补。
5. 按「怎么重启环境」搭好台，交棒给用户。

## 📍 现在在哪

- 进度：S1 ✅（已通关）；S2 ⬜（待开工）
- 当前部件图：真源在 `学习记录/S1 · 假镜像闭环.md` 顶部
- 进度：S1 ✅；S2 ✅（已通关）；S3 ⬜（待开工）
- ⏸ 手正悬在哪：**S2 通关落盘完成**；S3 开工前布局决策已拍板（内核 @0x11000000、DTB @0x11700000、分区 3M，分析见 `notes/DTB生命周期与布局分析.md`）；下一步 S3 开工仪式（立 S3 部件图：bootloader 加载真内核 + 最小 DTB + pl011 串口）
- 代码现状快照：构建拆分为 `s1/CMakeLists.txt`（bootloader + fake-image）+ `tests/CMakeLists.txt`（psram-test），根 CMakeLists 仅 add_subdirectory；Makefile 每个例子一条命令（`flash-bootloader` / `flash-fake` / `flash-psram-test` / `test` / `qemu`）
- 下一步动作：S3 开工仪式 → bootloader 拷贝 S1 骨架、加载真内核 Image 到 PSRAM、传 DTB 地址 → 最小 DTB（memory/uart）→ 板子串口出 banner
- QEMU 一键测试：`s2/run-qemu.sh`（参数：KERNEL_SRC / BUILD_DIR 可覆盖，额外参数透传给 qemu）
- 怎么重启环境：`cd /home/developer/iotahydrae/rp2350-linux`；`PICO_SDK_PATH=/home/developer/raspberrypi/pico-sdk`；工具链就绪后 `cmake -DPICO_PLATFORM=rp2350-riscv -B build -G Ninja`

## 🗺️ 要去哪

| 阶段 | 名字 | 跑成什么样 | 承重路标 | 状态 |
|---|---|---|---|---|
| S1 | 假镜像闭环 | bootloader 拷贝 + 跳转假镜像，串口打印确认 | PSRAM 初始化 + 跳转协议 | ✅ |
| S2 | QEMU 跑真内核 | 构建 riscv32 NOMMU 内核（MMU=n、M-mode），QEMU 里看到启动输出（哪怕 panic） | 内核构建 + 启动协议（入口/DTB/链接地址） | ✅ |
| S3 | 真板只接串口 | bootloader 加载真内核 Image 到 PSRAM + 最小 DTB（memory/uart）+ earlycon/console=pl011，板子串口出 banner | DTB + 内核加载 + pl011 earlycon | ⬜ |
| S3 | 真板只接串口 | 板子串口出内核第一行字 | 设备树 + 串口（pl011） | ⬜ |
| S4 | 真板接定时器 | 启动推进到新断点 | MTIME/MTIMECMP 驱动 | ⬜ |
| S5 | 真板接中断 | 内核能响应中断 | XH3IRQ 驱动 | ⬜ |
| S6 | 进 shell | 板子上敲命令 | initramfs + busybox | ⬜ |
| S7 | 待定 | 优化 / 更多外设 | 待定 | ⬜ |

- 认知模型待加固：
  - 「形状 vs 参数」抽象：迁移时哪些是结构、哪些是参数（S1 大考 Q2 没接住）→ S2/S3 真实施工练
  - 坑的排查顺序：先软件配置坑（地址/偏移/引脚），后硬件坑（S1 大考 Q3 先想到硬件）→ S2 练
  - 定时器/中断子系统尚未接触（S2 大考 Q1 自认不懂）→ S4/S5 补
  - DTB 生命周期与布局 → 已拍板并成文（`notes/DTB生命周期与布局分析.md`），S3 按此落地
- 手边比喻世界：（空）

## 🎯 这是什么

- 目标：Linux（riscv32 NOMMU）跑在 Waveshare RP2350 Plus 16MB 的 RISC-V 核上，串口进 shell
- 剧本：烧录 → 上电日志 → 里程碑（假镜像 → 真内核 → shell）
- 代码：`/home/developer/iotahydrae/rp2350-linux`（PLAN.md）；习得：`notes/学习记录/`
- 能力块：`../_能力库.md`（Documents 笔记根，跨项目）
- 用户画像：懂 Linux 驱动与 pico-sdk；移植经验为零；要求小步实验、每关记录 + git 提交；中文交流；会 picotool / openocd / 真板调试
- 用户约定：**需求/项目结构有变化时，同步更新仓库根 `AGENTS.md`**（2026-08-21 用户要求；AGENTS.md 由 `codex /init` 生成基础版 + AI 补充项目约定）
- 参考速查：`notes/RISC-V调试与反汇编速查.md`（异常三件套 + 反汇编命令 + 案例复盘）
- 参考速查：`notes/RISC-V汇编指令速查.md`（ARM 对照版：寄存器 / 指令表 / 压缩指令 / 反汇编阅读）
- 环境速查：`notes/环境搭建.md`（RISC-V 工具链下载/安装 + 完整构建环境）
- DTB 参考：`notes/QEMU-virt-DTB参考.md`（QEMU 动态生成，dumpdtb 导出；关键节点 ↔ 启动日志）
- DTB 生命周期：`notes/DTB生命周期与布局分析.md`（解析后数据已死但内存被内核永久保留；memory 节点 = 内核唯一内存信息来源；SRAM/SCRATCH/XIP_RAM 暂不声明、闲置 → S3 布局依据）
- 练习题：`exercises/`（S1/S2 知识题/代码题/程序题，答案折叠；S3+ 随阶段补充）
- 原始日志：`notes/实验日志/`（真机原始输出存档，带日期/板子/结论）
